version: '3.8'

services:
  redis:
    image: redis:7.0-alpine
    container_name: url_shortener_redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: always

volumes:
  redis-data:
